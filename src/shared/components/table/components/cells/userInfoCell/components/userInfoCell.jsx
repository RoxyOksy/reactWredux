import React from 'react';
import {UserBlock} from '../../../../../../../modules/user/components/userView/components/userBlock';

const UserInfoCellComponent = (props)=>{
  return <UserBlock {...props}/>
};

export default UserInfoCellComponent
