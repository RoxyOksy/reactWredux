import React from "react";

import styles from './userBlock.module.scss';

const UserBlockComponent = ({image}) => (
    <div className={styles.userBlock}>

    </div>
  );

export default UserBlockComponent
